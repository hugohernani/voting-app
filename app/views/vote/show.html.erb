<header class="masthead d-flex">
  <div class="container text-center my-auto">
    <h1 class="mb-1">Congratulations!</h1>
    <h3 class="mb-5">
      <p>
        You can check the election result here:
          <%= link_to @election.name, election_path(@election.id) %>
          after <%= t(@election.end_time) %> which is when the election ends.</p>
      </p>
    </h3>
  </div>
  <div class="overlay"></div>
</header>
